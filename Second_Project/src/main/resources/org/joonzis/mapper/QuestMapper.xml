<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.joonzis.mapper.QuestMapper">

    <!-- <insert id="insertQuest" parameterType="org.joonzis.domain.QuestRequestVO">
        INSERT INTO question_request (
            id,
            subject,
            question_text,
            option_1,
            option_2,
            option_3,
            option_4,
            correct_answer,
            image_data,
            user_no,
            REQUEST_CREATED_AT
        ) VALUES (
            seq_questRequest.NEXTVAL,
            #{subject},
            #{question_text},
            #{option_1},
            #{option_2},
            #{option_3},
            #{option_4},
            #{correct_answer},
            #{image_data, jdbcType=BLOB, typeHandler=org.apache.ibatis.type.ByteArrayTypeHandler},
            #{user_no},
            SYSTIMESTAMP
        )
    </insert> -->
    <insert id="insertQuest">
    INSERT INTO question_request (
        id,
        subject,
        question_text,
        option_1,
        option_2,
        option_3,
        option_4,
        correct_answer,
        image_data,
        user_no,
        REQUEST_CREATED_AT
    ) VALUES (
        seq_questRequest.NEXTVAL,
        #{subject},
        #{question_text},
        #{option_1},
        #{option_2},
        #{option_3},
        #{option_4},
        #{correct_answer},
        #{image_data, jdbcType=BLOB},
        #{user_no},
        SYSTIMESTAMP
    )
</insert>

</mapper>