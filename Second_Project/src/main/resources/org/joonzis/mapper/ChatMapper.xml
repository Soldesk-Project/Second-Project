<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.joonzis.mapper.ChatMapper">
	<insert id="reportHistory" parameterType="org.joonzis.domain.ReportHistoryVO">
		INSERT INTO REPORT_HISTORY VALUES (REPORT_HISTORY_SEQ.NEXTVAL, #{reported_user}, #{message}, #{reason, jdbcType=CHAR}, #{status}, SYSDATE)
	</insert>  

	<select id="getReportHistory" resultType="org.joonzis.domain.ReportHistoryVO" parameterType="map">
		<![CDATA[
		  SELECT * FROM (
		    SELECT a.*, ROWNUM rnum FROM (
		      SELECT * FROM REPORT_HISTORY ORDER BY REPORTED_AT DESC
		    ) a WHERE ROWNUM <= #{offset} + #{size}
		  ) WHERE rnum > #{offset}
		]]>
	</select>
		
	<select id="getReportHistoryCount" resultType="int">
		SELECT COUNT(*) FROM REPORT_HISTORY
	</select>
  
  
  
  
</mapper>
