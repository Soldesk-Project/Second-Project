<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.joonzis.mapper.ShopMapper">
	 
	<!-- 상점 - 카테고리별 아이템 목록 -->
	<select id="getItemsByCategories" resultType="org.joonzis.domain.ItemVO" parameterType="list">
		SELECT
			ITEM_NO            AS item_no,
			ITEM_PRICE         AS item_price,
			ITEM_NAME          AS item_name,
			ITEM_TYPE          AS item_type,
			IMAGE_FILE_NAME    AS imageFileName,
			CSS_CLASS_NAME     AS css_class_name
		FROM ITEMS
		WHERE ITEM_TYPE IN
		<foreach collection="list" item="category" open="(" separator="," close=")">
			#{category}
		</foreach>
	</select>
</mapper>